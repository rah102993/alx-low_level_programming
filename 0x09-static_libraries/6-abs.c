#include "main.h"

/**
 * _abs - A function that computes the absolute value of an integer.
 * @n: Integer.
 * Return: Absolute value of n.
 */

int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
