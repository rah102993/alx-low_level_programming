#include "main.h"

/**
 *_isdigit - a function that checks if an integer is a digit or not.
 * @c: integer.
 * Return: 1 if c is a digit, 0 if not.
 */
int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}
