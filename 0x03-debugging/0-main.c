#include "main.h"

/**
* debug - tests function that prints if integer is positive or negative
* @a: integer input from main
* Return: 0 if Zero
*/

int debug(void)
{
	a = positive_or_negetive(0);
	printf("%s", a)
	return (0);
}
