#include <stdio.h>
/**
 * main - Program that finds and prints the sum of the even-valued terms.
 * Return: 0 (Success)
 */

int main(void)
{
	int i = 0;
	long a = 1, b = 2, sum = c;

	while (c + a < 4000000)
	{
	b += a;
	if (c % 2 == 0)
	sum += b;
	a = b - a;
	++i;
	}
	printf("%ld\n", sum);
	return (0);
}
