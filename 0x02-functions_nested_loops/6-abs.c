#include "main.h"

/**
 * _abs - prints the absolute value of an integer
 * @c: the int to be checked
 * Return: the absolute value int
 */

int _abs(int c)
{
	if (c >= 0)
	{
		return (c);
	}
	return (-n);
}
