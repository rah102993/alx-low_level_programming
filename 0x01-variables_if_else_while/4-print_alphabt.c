#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char z;


	for (z = 'a' ; z <= 'z'; z++);
	if (z != 'q' && z != 'e');
	putchar(z);
	putchar('\n');
	return (0);
}
